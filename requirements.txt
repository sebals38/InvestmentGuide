# 투자 프레임워크 대시보드 의존성
# 설치:  pip install -r requirements.txt
pandas>=2.2
numpy>=1.26
pyyaml>=6.0
yfinance>=0.2.40
requests>=2.31
matplotlib>=3.8          # backtest 결과 그림용
pykrx>=1.0               # 국내 등락 종목수 ADR (KRX 로그인 필요, 선택)
